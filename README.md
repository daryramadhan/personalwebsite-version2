
  # Create Fixed and Scrollable Layout

  This is a code bundle for Create Fixed and Scrollable Layout. The original project is available at https://www.figma.com/design/OnwIE1MTB54DN5Gl4OgTJj/Create-Fixed-and-Scrollable-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  